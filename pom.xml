<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.github.indiv0</groupId>
  <artifactId>pearlnerf</artifactId>
  <version>1.0.0</version>
  <name>PearlNerf</name>
  <description>A plugin to nerf enderpearl functionality on PVP-based servers.</description>

  <properties>
    <altDeploymentRepository>-DaltDeploymentRepository=repo::default::file:../indiv0-mvn-repo/releases</altDeploymentRepository>
    <bukkitutils.version>1.0.3</bukkitutils.version>
    <combattag.version>1.0.0</combattag.version>
  </properties>

  <parent>
    <groupId>ashulman</groupId>
    <artifactId>maven-parent</artifactId>
    <version>0.3.3</version>
  </parent>

  <distributionManagement>
    <repository>
      <id>repo</id>
      <url>https://github.com/Indiv0/indiv0-mvn-repo/raw/master/releases/</url>
    </repository>
  </distributionManagement>

  <dependencies>
    <!-- Bukkit -->
    <dependency>
      <groupId>org.bukkit</groupId>
      <artifactId>bukkit</artifactId>
    </dependency>
    <!-- BukkitUtils -->
    <dependency>
      <groupId>com.github.indiv0</groupId>
      <artifactId>BukkitUtils</artifactId>
      <version>${bukkitutils.version}</version>
    </dependency>
    <!-- CombatTag -->
    <dependency>
      <groupId>com.trc202</groupId>
      <artifactId>CombatTag</artifactId>
      <version>${combattag.version}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>copy-to-local-server</id>
            <phase>deploy</phase>
            <goals>
              <goal>copy</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>${project.groupId}</groupId>
                  <artifactId>${project.artifactId}</artifactId>
                  <version>${project.version}</version>
                  <type>${project.packaging}</type>
                </artifactItem>
              </artifactItems>
              <outputDirectory>F:/Server/plugins</outputDirectory>
              <stripVersion>true</stripVersion>                    
            </configuration>
          </execution>                        
        </executions>
      </plugin>
    </plugins>
  </build>
</project>